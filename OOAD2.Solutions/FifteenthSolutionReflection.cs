namespace OOAD2.Solutions;

public class FifteenthSolutionReflection
{
  // Анализируя своё решение, я заметил, что оно хорошо показывает главную идею задачи — замену поля paymentType со значениями
  // "cash"/"card"/"online" на отдельные классы.В отличие от Java-примера, где классы Junior/Middle/Senior оказались совсем пустыми,
  // у меня каждый тип платежа действительно делает что-то своё: считает комиссию по-разному, по-разному обрабатывается.Это показывает,
  // зачем вообще нужно наследование, а не просто if (paymentType == "cash"). Python-пример проще моего, но там логика почти ничего не делает.
  // Минус моего решения в том, что я добавил много деталей(маскирование карт, вывод в консоль), которые делают код длиннее и могут отвлекать
  // от основной идеи.Для учебного примера можно было обойтись более простой реализацией методов.
}